services:
  seafile-mcp:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SEAFILE_TRANSPORT: streamable-http
      SEAFILE_HOST: 0.0.0.0
      SEAFILE_PORT: 8000
    restart: unless-stopped
