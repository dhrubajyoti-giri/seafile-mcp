services:
  seafile-mcp:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      SEAFILE_TRANSPORT: streamable-http
      SEAFILE_HOST: 0.0.0.0
      SEAFILE_PORT: 8000
    restart: unless-stopped
